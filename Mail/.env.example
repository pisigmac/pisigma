MAIL_ADMIN_TOKEN=change-me-admin
MAIL_PROVIDER=console
MAIL_FROM=PlexApps <noreply@plexapps.com>

# SMTP (production) — your Postfix / Mailcow / SES SMTP / etc.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=0
SMTP_STARTTLS=1

# Optional: pisigma-auth for /v1/admin (not Clerk)
# MAIL_AUTH_JWKS_URL=http://127.0.0.1:8090/.well-known/jwks.json
# MAIL_AUTH_ISSUER=https://auth.pisigma.local
# MAIL_AUTH_AUDIENCE=mail
